[
  {
    "acId": "AC-1",
    "original": "IncrementalGraphDiffService.diffAndApply() computes a diff and applies only the delta (added, modified, removed nodes/links)",
    "refined": "Method `diffAndApply(projectId: string, newNodes: GraphifyNodeDto[], newLinks: GraphifyLinkDto[]): Promise<DiffResult>` exists on `IncrementalGraphDiffService` class. Return value contains numeric properties: added (number of nodes inserted), updated (number of nodes modified), removed (number of nodes deleted), indexed (number of nodes written to LanceDB), durationMs (elapsed time in ms). No `deleteAll` operation is called on the full graph.",
    "testable": true,
    "storyId": "US-001"
  },
  {
    "acId": "AC-2",
    "original": "Stored graph is loaded from `GraphNode` and `GraphLink` Prisma tables, not from LanceDB",
    "refined": "When `diffAndApply` executes, it calls `graphStore.getStoredGraph(projectId)` which returns `StoredGraph` with `nodeMap: Map<string, GraphifyNodeDto>` and `linkMap: Map<string, GraphifyLinkDto[]>`. The implementation queries `prisma.graphNode.findMany({ where: { projectId } })` and `prisma.graphLink.findMany({ where: { projectId } })`. No LanceDB read operations occur during graph loading.",
    "testable": true,
    "storyId": "US-001"
  },
  {
    "acId": "AC-3",
    "original": "When `newNodes` has 100 nodes and stored has 90 matching nodes, `DiffResult.added` equals 10 and `DiffResult.removed` equals 0",
    "refined": "Test setup: stored contains 90 nodes where all 90 have matching nodeIds in incoming 100-node set (same label, type, sourceFile, outgoing links). `diffAndApply` returns `DiffResult` where `added === 10` and `removed === 0`. The 10 added nodes are absent from stored graph; the 90 stored nodes are matched and not re-indexed.",
    "testable": true,
    "storyId": "US-001"
  },
  {
    "acId": "AC-4",
    "original": "When a stored node is absent from `newNodes`, it is deleted from both the `GraphNode` Prisma table and LanceDB (by graphify nodeId / RAG sourceId)",
    "refined": "Given stored node with `nodeId = 'n1'` absent from `newNodes` array, after `diffAndApply`: (1) `prisma.graphNode.delete({ where: { id: 'n1' } })` is called, (2) `graphStore.deleteFromVectorStore('n1')` is called with 'n1' as the RAG sourceId. Verification: query `prisma.graphNode.findUnique({ where: { id: 'n1' } })` returns null; LanceDB document with sourceId='n1' is deleted.",
    "testable": true,
    "storyId": "US-001"
  },
  {
    "acId": "AC-5",
    "original": "When a node exists in both stored and incoming with unchanged content (label, type, sourceFile, outgoing links), it is skipped with no re-index into LanceDB",
    "refined": "Given stored node with `nodeId='n1', label='Foo', type='function', sourceFile='/a.ts'` and incoming node with same `nodeId, label, type, sourceFile, outgoingLinks`, after `diffAndApply`: (1) `prisma.graphNode.update` is NOT called, (2) LanceDB upsert is NOT called for 'n1'. `DiffResult.updated` does not increment for this node. Verification: spy on LanceDB upsert - no call with sourceId='n1'.",
    "testable": true,
    "storyId": "US-001"
  },
  {
    "acId": "AC-6",
    "original": "`diffAndApply` is called by the existing `importGraphify` endpoint, replacing the current `deleteAllBySourceType` logic",
    "refined": "POST `/code-intel/import-graphify` endpoint (1) receives `ImportGraphifyDto` with `projectId, nodes[], links[]`, (2) calls `incrementalGraphDiffService.diffAndApply(projectId, nodes, links)` instead of `rag.deleteAllBySourceType('code')`, (3) returns `DiffResult` serialized as JSON with fields: added, updated, removed, indexed, durationMs. HTTP status is 200 on success.",
    "testable": true,
    "storyId": "US-001"
  },
  {
    "acId": "AC-7",
    "original": "`DiffResult.indexed` reflects only the number of nodes actually written to LanceDB (added + updated), not the total node count",
    "refined": "When `diffAndApply` returns `DiffResult`: (1) `indexed === added + updated` holds true, (2) `indexed <= newNodes.length` always, (3) if all 100 incoming nodes are unchanged from stored, `indexed === 0`. Verification: compare `indexed` value against spy count of LanceDB upsert calls.",
    "testable": true,
    "storyId": "US-001"
  },
  {
    "acId": "AC-8",
    "original": "A diff-and-apply of 500 unchanged nodes + 10 added + 5 removed completes in under 2 seconds",
    "refined": "Test with 515 stored nodes and incoming payload of 510 nodes (500 unchanged + 10 added) + existing 5 nodes absent from incoming. Timer starts before `diffAndApply` call and stops on resolution. Assert `result.durationMs < 2000`. Warm run (after any connection pooling startup) is measured, not cold bootstrap.",
    "testable": true,
    "storyId": "US-001"
  },
  {
    "acId": "AC-9",
    "original": "`DiffResult` includes `durationMs` measuring the total diff-and-apply time",
    "refined": "Interface `DiffResult` includes required field `durationMs: number`. After any `diffAndApply` call, `result.durationMs` is a positive integer representing milliseconds from method entry to method return, including all Prisma and LanceDB operations.",
    "testable": true,
    "storyId": "US-001"
  },
  {
    "acId": "AC-10",
    "original": "Existing graphify imports remain project-scoped and continue to honor `Project.graphifyEnabled`",
    "refined": "When `importGraphify` endpoint receives request: (1) `ProjectService.findOne(projectId)` is called, (2) if `project.graphifyEnabled === false`, method returns early with HTTP 200 and `DiffResult` where all counts are 0 (no changes applied), (3) all `GraphNode` and `GraphLink` records created/modified/deleted have `projectId === input.projectId`. No cross-project graph mutations occur.",
    "testable": true,
    "storyId": "US-001"
  },
  {
    "acId": "AC-11",
    "original": "The `importGraphify` route is gated with `@RequiredPermission([KodaAction.IMPORT, 'CodeIntel'])`. The factory grants this permission to ADMIN users, all agents, and users with `DEVELOPER` project role (when project membership is implemented). Non-permitted callers receive 403 from `PermissionAuthGuard` with no inline check in the controller",
    "refined": "Endpoint `POST /code-intel/import-graphify` has `@RequiredPermission([KodaAction.IMPORT, 'CodeIntel'])` decorator. (1) Request with missing/invalid JWT returns HTTP 401, (2) Request from user lacking IMPORT permission on CodeIntel subject returns HTTP 403 with body `{ statusCode: 403, message: 'Forbidden' }`, (3) No permission check code exists inside the controller method itself - guard handles it. Integration test: mock CASL ability where user can IMPORT CodeIntel → expect 200; deny IMPORT → expect 403.",
    "testable": true,
    "storyId": "US-001"
  },
  {
    "acId": "AC-12",
    "original": "`AstIndexService.indexCommit()` parses source files and stores symbol metadata in the `Symbol` Prisma table",
    "refined": "`AstIndexService.indexCommit()` parses source files and stores symbol metadata in the `Symbol` Prisma table",
    "testable": true,
    "storyId": "US-002"
  },
  {
    "acId": "AC-13",
    "original": "`Symbol.symbolId` uses the convention `{repoId}:{filePath}::{SymbolName}` and is globally unique within a project",
    "refined": "`Symbol.symbolId` uses the convention `{repoId}:{filePath}::{SymbolName}` and is globally unique within a project",
    "testable": true,
    "storyId": "US-002"
  },
  {
    "acId": "AC-14",
    "original": "`getCallers(symbolId)` returns all symbols that include the given `symbolId` in their JSON `callers` list",
    "refined": "`getCallers(symbolId)` returns all symbols that include the given `symbolId` in their JSON `callers` list",
    "testable": true,
    "storyId": "US-002"
  },
  {
    "acId": "AC-15",
    "original": "`getCallees(symbolId)` returns all symbols listed in the given symbol's JSON `callees` list",
    "refined": "`getCallees(symbolId)` returns all symbols listed in the given symbol's JSON `callees` list",
    "testable": true,
    "storyId": "US-002"
  },
  {
    "acId": "AC-16",
    "original": "When `indexCommit` is called with `files=[{path: 'src/auth.ts', content: '...'}]`, only `src/auth.ts` is parsed and existing symbols for unchanged files are preserved",
    "refined": "When `indexCommit` is called with `files=[{path: 'src/auth.ts', content: '...'}]`, only `src/auth.ts` is parsed and existing symbols for unchanged files are preserved",
    "testable": true,
    "storyId": "US-002"
  },
  {
    "acId": "AC-17",
    "original": "`Symbol.signature` captures parameter types and return type in a format such as `(userId: string): Promise<User>`",
    "refined": "`Symbol.signature` captures parameter types and return type in a format such as `(userId: string): Promise<User>`",
    "testable": true,
    "storyId": "US-002"
  },
  {
    "acId": "AC-18",
    "original": "Indexing is triggered by a `code_commit` outbox event, which is fired by the VCS webhook handler (see US-004)",
    "refined": "Indexing is triggered by a `code_commit` outbox event, which is fired by the VCS webhook handler (see US-004)",
    "testable": true,
    "storyId": "US-002"
  },
  {
    "acId": "AC-19",
    "original": "A commit with 20 files (average 200 lines each) is indexed in under 30 seconds",
    "refined": "A commit with 20 files (average 200 lines each) is indexed in under 30 seconds",
    "testable": true,
    "storyId": "US-002"
  },
  {
    "acId": "AC-20",
    "original": "Direct calls to the `indexCommit()` controller endpoint are gated with `@RequiredPermission([KodaAction.MANAGE, 'AstIndex'])` and the factory grants this to ADMIN users and agents with DEVELOPER role only; other callers receive 403 from PermissionAuthGuard",
    "refined": "Direct calls to the `indexCommit()` controller endpoint are gated with `@RequiredPermission([KodaAction.MANAGE, 'AstIndex'])` and the factory grants this to ADMIN users and agents with DEVELOPER role only; other callers receive 403 from PermissionAuthGuard",
    "testable": true,
    "storyId": "US-002"
  },
  {
    "acId": "AC-21",
    "original": "Parser failures for one file are recorded in `SymbolIndexResult.fileErrors` and do not prevent other files in the same commit from being indexed",
    "refined": "Parser failures for one file are recorded in `SymbolIndexResult.fileErrors` and do not prevent other files in the same commit from being indexed",
    "testable": true,
    "storyId": "US-002"
  },
  {
    "acId": "AC-22",
    "original": "The `code_commit` outbox handler resolves changed file contents via the VCS provider before calling `indexCommit()`; the webhook controller only enqueues commit metadata",
    "refined": "The `code_commit` outbox handler resolves changed file contents via the VCS provider before calling `indexCommit()`; the webhook controller only enqueues commit metadata",
    "testable": true,
    "storyId": "US-002"
  },
  {
    "acId": "AC-23",
    "original": "`EntityGraphService.rebuildGraph(projectId)` rebuilds all entity nodes and links for a project from existing data",
    "refined": "`EntityGraphService.rebuildGraph(projectId)` rebuilds all entity nodes and links for a project from existing data",
    "testable": true,
    "storyId": "US-003"
  },
  {
    "acId": "AC-24",
    "original": "Entity graph data is persisted in `EntityNode` and `EntityLink` Prisma tables, not only in Phase 2's in-memory `EntityStore`",
    "refined": "Entity graph data is persisted in `EntityNode` and `EntityLink` Prisma tables, not only in Phase 2's in-memory `EntityStore`",
    "testable": true,
    "storyId": "US-003"
  },
  {
    "acId": "AC-25",
    "original": "`onTicketEvent(status_changed)` updates the entity node for that ticket if it exists in the entity graph",
    "refined": "`onTicketEvent(status_changed)` updates the entity node for that ticket if it exists in the entity graph",
    "testable": true,
    "storyId": "US-003"
  },
  {
    "acId": "AC-26",
    "original": "`onGraphifyImport()` extracts `service` entity nodes from graphify nodes with `type=code_module`",
    "refined": "`onGraphifyImport()` extracts `service` entity nodes from graphify nodes with `type=code_module`",
    "testable": true,
    "storyId": "US-003"
  },
  {
    "acId": "AC-27",
    "original": "Service linkage works from current Koda fields (gitRefFile, labels, linked code/module references) without requiring a `Ticket.serviceId` column",
    "refined": "Service linkage works from current Koda fields (gitRefFile, labels, linked code/module references) without requiring a `Ticket.serviceId` column",
    "testable": true,
    "storyId": "US-003"
  },
  {
    "acId": "AC-28",
    "original": "`getRelatedEntities(projectId, entityId, depth=2)` returns all entity nodes reachable within 2 hops from the given entity",
    "refined": "`getRelatedEntities(projectId, entityId, depth=2)` returns all entity nodes reachable within 2 hops from the given entity",
    "testable": true,
    "storyId": "US-003"
  },
  {
    "acId": "AC-29",
    "original": "`getIncidentImpact(projectId, incidentTicketId)` returns all entity nodes linked to the incident ticket",
    "refined": "`getIncidentImpact(projectId, incidentTicketId)` returns all entity nodes linked to the incident ticket",
    "testable": true,
    "storyId": "US-003"
  },
  {
    "acId": "AC-30",
    "original": "Service entities extracted from graphify inherit `tags` from the graphify node (e.g. `['backend', 'auth']`)",
    "refined": "Service entities extracted from graphify inherit `tags` from the graphify node (e.g. `['backend', 'auth']`)",
    "testable": true,
    "storyId": "US-003"
  },
  {
    "acId": "AC-31",
    "original": "Entity graph is updated incrementally via outbox fan-out handlers; no full rebuild is performed on every event",
    "refined": "Entity graph is updated incrementally via outbox fan-out handlers; no full rebuild is performed on every event",
    "testable": true,
    "storyId": "US-003"
  },
  {
    "acId": "AC-32",
    "original": "A graph with 500 nodes and 2000 edges returns `getRelatedEntities` in under 50ms",
    "refined": "A graph with 500 nodes and 2000 edges returns `getRelatedEntities` in under 50ms",
    "testable": true,
    "storyId": "US-003"
  },
  {
    "acId": "AC-33",
    "original": "VcsWebhookController handles push events at POST /projects/:slug/vcs-webhook in addition to existing issue and PR events",
    "refined": "GET /projects/:slug/vcs-webhook returns 404 (not implemented) or route not registered for GET; POST /projects/:slug/vcs-webhook with push event payload returns 2xx and routes to push handler; VcsWebhookService.handlePush() is called when event type is 'push'",
    "testable": true,
    "storyId": "US-004"
  },
  {
    "acId": "AC-34",
    "original": "Webhook payload is validated for presence of repository identifier, ref, commits[] array, and sender identifier (sender.id, sender.login, or provider equivalent)",
    "refined": "POST /projects/:slug/vcs-webhook with missing repository identifier returns 400 with error body; POST /projects/:slug/vcs-webhook with missing ref returns 400; POST /projects/:slug/vcs-webhook with missing commits[] returns 400; POST /projects/:slug/vcs-webhook with missing sender.id and sender.login returns 400; payload with all fields present extracts repoId, ref, commits array, and sender correctly",
    "testable": true,
    "storyId": "US-004"
  },
  {
    "acId": "AC-35",
    "original": "For each commit in the push payload, a code_commit outbox event is enqueued with payload { repoId, commitHash, ref, changedFiles }",
    "refined": "OutboxService.enqueue() is called N times for push payload with N commits; each enqueued payload has fields: type='code_commit', repoId matches repository identifier, commitHash matches commit.sha, ref matches push ref, changedFiles matches commit.changed_files array; enqueue returns before indexCommit executes",
    "testable": true,
    "storyId": "US-004"
  },
  {
    "acId": "AC-36",
    "original": "AstIndexService.indexCommit() is called by the code_commit outbox handler, not synchronously by the webhook controller",
    "refined": "VcsWebhookController.handlePush() does not call AstIndexService.indexCommit(); CodeCommitOutboxHandler.process() calls AstIndexService.indexCommit() with (repoId, commitHash, sourceFiles); outbox handler is registered and processes code_commit events asynchronously",
    "testable": true,
    "storyId": "US-004"
  },
  {
    "acId": "AC-37",
    "original": "If OutboxService.enqueue() fails, the webhook logs the failure and returns a non-2xx response so the provider can retry",
    "refined": "When OutboxService.enqueue() throws or returns error, response status is >= 400; error log contains 'enqueue' and 'code_commit' keywords; provider receives non-2xx within request timeout",
    "testable": true,
    "storyId": "US-004"
  },
  {
    "acId": "AC-38",
    "original": "GitHub webhook signature verification reuses the existing x-hub-signature-256 HMAC-SHA256 flow; invalid signatures return 401",
    "refined": "POST /projects/:slug/vcs-webhook without x-hub-signature-256 header returns 401; POST with invalid signature returns 401; POST with valid HMAC-SHA256 signature (computed from payload using VcsConnection.secret) returns 2xx; signature verification uses crypto.timingSafeEqual for comparison",
    "testable": true,
    "storyId": "US-004"
  },
  {
    "acId": "AC-39",
    "original": "The webhook secret continues to come from the project's VcsConnection record and is never hard-coded",
    "refined": "VcsConnection.findFirst() or findUnique() is called to fetch webhook secret; no hardcoded secret strings exist in VcsWebhookController or VcsWebhookService; secret value comes from database query with projectId filter",
    "testable": true,
    "storyId": "US-004"
  },
  {
    "acId": "AC-40",
    "original": "Pushing the same commitHash twice within a 5-minute window does not create duplicate outbox events (deduplication enforced)",
    "refined": "First push of commitHash calls OutboxService.enqueue() once; second push of same commitHash within 300 seconds calls OutboxService.enqueue() zero times; deduplication uses commitHash + time window; after 5 minutes, same commitHash enqueues again",
    "testable": true,
    "storyId": "US-004"
  },
  {
    "acId": "AC-41",
    "original": "GET /projects/:slug/codeintel/impact?repoId=X&commitHash=Y&changedFiles=file1,file2 returns a ChangeImpactResult with the expected schema fields",
    "refined": "HTTP GET /projects/:slug/codeintel/impact returns status 200 with Content-Type: application/json and a JSON body containing all required fields: commitHash (string), changedFiles (string array), impactedSymbols (array with {id, name, file, type}), impactedServices (array with {id, type, name}), impactedTickets (array with {id, type, title}), impactScore (number), and provenance (object with sourceTicketId when ticketId param is provided). Non-permitted requests return 403 with {statusCode: 403, message: 'Forbidden'}",
    "testable": true,
    "storyId": "US-005"
  },
  {
    "acId": "AC-42",
    "original": "impactedSymbols contains symbols whose file property matches any entry in the changedFiles query parameter",
    "refined": "Every symbol s in impactedSymbols satisfies: changedFiles.includes(s.file). AND impactedSymbols contains zero symbols where s.file is not in changedFiles. AND for each file in changedFiles, at least one symbol with that file exists in impactedSymbols when symbols for that file exist in the SymbolStore index.",
    "testable": true,
    "storyId": "US-005"
  },
  {
    "acId": "AC-43",
    "original": "impactedServices contains service entity nodes linked to impacted symbols through matching Symbol.file to GraphNode.sourceFile or EntityNode.sourceId",
    "refined": "Every service in impactedServices has type === 'SERVICE'. AND for each impacted symbol s, there exists a GraphNode where (node.sourceFile === s.file OR node.sourceId === s.file) AND node.type === 'SERVICE' AND impactedServices includes node. AND impactedServices contains zero non-SERVICE entity nodes. AND all services in impactedServices are reachable through Symbol.file → GraphNode.sourceFile/sourceId chain from impactedSymbols.",
    "testable": true,
    "storyId": "US-005"
  },
  {
    "acId": "AC-44",
    "original": "impactedTickets contains ticket entity nodes linked to any impactedServices",
    "refined": "Every ticket in impactedTickets has type matching /TICKET|INCIDENT|TASK/. AND for each ticket t in impactedTickets, there exists a service s in impactedServices where t is linked to s through entity graph edges. AND impactedTickets contains zero non-ticket entity nodes. AND all tickets in impactedTickets are reachable from impactedServices through entity graph links.",
    "testable": true,
    "storyId": "US-005"
  },
  {
    "acId": "AC-45",
    "original": "impactScore is a number between 0 and 100 computed by the formula: 0.3 × (affectedSymbols / totalSymbols × 100) + 0.3 × (affectedServices / totalServices × 100) + 0.2 × (affectedTickets / totalTickets × 100) + 0.2 × (linkedIncidents > 0 ? 100 : 0)",
    "refined": "impactScore is a finite number. impactScore >= 0 AND impactScore <= 100. impactScore === Math.min(100, Math.max(0, 0.3 * symbolTerm + 0.3 * serviceTerm + 0.2 * ticketTerm + 0.2 * incidentTerm)). Where symbolTerm = totalSymbols > 0 ? (affectedSymbols / totalSymbols * 100) : 0, serviceTerm = totalServices > 0 ? (affectedServices / totalServices * 100) : 0, ticketTerm = totalTickets > 0 ? (affectedTickets / totalTickets * 100) : 0, incidentTerm = linkedIncidents > 0 ? 100 : 0. Zero-denominator case: term contributes 0 not NaN.",
    "testable": true,
    "storyId": "US-005"
  },
  {
    "acId": "AC-46",
    "original": "When called with a ticketId query parameter, the result includes provenance source metadata pointing to that ticket",
    "refined": "When query includes ticketId=X, result.provenance exists AND result.provenance.sourceTicketId === X AND result.provenance.sourceType === 'TICKET'. provenance object contains at minimum: {sourceTicketId: string, sourceType: 'TICKET', linkedAt: ISO8601 timestamp}. When ticketId param is absent, provenance may be null or omitted.",
    "testable": true,
    "storyId": "US-005"
  },
  {
    "acId": "AC-47",
    "original": "The endpoint returns a response in under 5 seconds for a commit touching up to 50 changed files",
    "refined": "GET /projects/:slug/codeintel/impact with changedFiles containing up to 50 entries completes in < 5000ms. Measured from request headers received to response body sent. Test uses representative dataset with at least 100 symbols, 50 services, 20 tickets in the indexes.",
    "testable": true,
    "storyId": "US-005"
  },
  {
    "acId": "AC-48",
    "original": "The endpoint is gated with @RequiredPermission([KodaAction.READ, 'CodeIntel']). The factory grants this to ADMIN users, all agents, and users with DEVELOPER project role (when project membership is implemented). Controller uses @Principal() principal: KodaPrincipal; non-permitted callers receive 403 from PermissionAuthGuard with no inline role check",
    "refined": "Endpoint decorator @RequiredPermission([KodaAction.READ, 'CodeIntel']) is present on the handler. Request with principal.role === 'ADMIN' returns 200. Request from agent principal returns 200. Request with principal.role === 'DEVELOPER' and principal.projects includes projectSlug returns 200. Request with principal.role !== 'ADMIN' AND principal.role !== 'AGENT' AND NOT (role === 'DEVELOPER' AND project membership) returns 403. Response body is {statusCode: 403, message: 'Forbidden'} from PermissionAuthGuard, not an inline role check. No role switch/case logic exists in the controller method body.",
    "testable": true,
    "storyId": "US-005"
  },
  {
    "acId": "AC-49",
    "original": "CanonicalStateService.getSnapshot(query) returns retrievedAt set to the server timestamp at the time of snapshot creation",
    "refined": "The returned CanonicalSnapshot has retrievedAt property where: retrievedAt instanceof Date === true AND (current_server_time - retrievedAt) < 1000ms",
    "testable": true,
    "storyId": "US-006"
  },
  {
    "acId": "AC-50",
    "original": "When ticketIds are provided, tickets contains only tickets belonging to the requested projectId and matching the given IDs",
    "refined": "Given a query with projectId='P1' and ticketIds=['T1','T2']: assert snapshot.tickets.length === 2 AND every ticket has ticket.projectId === 'P1' AND ticket.id is in ['T1','T2']",
    "testable": true,
    "storyId": "US-006"
  },
  {
    "acId": "AC-51",
    "original": "When ticketIds is empty or undefined, the tickets array is empty",
    "refined": "Given getSnapshot({projectId:'P1'}) with no ticketIds: assert Array.isArray(snapshot.tickets) === true AND snapshot.tickets.length === 0",
    "testable": true,
    "storyId": "US-006"
  },
  {
    "acId": "AC-52",
    "original": "When timeWindow is provided, recentEvents contains only events within the time window, ordered by createdAt descending",
    "refined": "Given timeWindow={from:2024-01-01, to:2024-01-02}: assert every event in snapshot.recentEvents has event.createdAt >= 2024-01-01 AND event.createdAt <= 2024-01-02 AND for i>0: snapshot.recentEvents[i-1].createdAt >= snapshot.recentEvents[i].createdAt",
    "testable": true,
    "storyId": "US-006"
  },
  {
    "acId": "AC-53",
    "original": "activeDecisions is populated from active MemoryItem rows where kind='DECISION' and status='active' in the project",
    "refined": "Assert snapshot.activeDecisions every item has item.kind === 'DECISION' AND item.status === 'active' AND item.projectId === query.projectId AND items correspond to MemoryItem rows in database with those exact conditions",
    "testable": true,
    "storyId": "US-006"
  },
  {
    "acId": "AC-54",
    "original": "When a non-existent or unauthorized projectId is requested, the service throws a ForbiddenAppException or NotFoundAppException matching the Nathapp exception style used by existing memory endpoints",
    "refined": "Calling getSnapshot({projectId:'nonexistent'}) throws an error where: error constructor.name === 'NotFoundAppException' OR error.constructor.name === 'ForbiddenAppException' AND error.message contains 'projectId' OR 'not found' OR 'forbidden'",
    "testable": true,
    "storyId": "US-006"
  },
  {
    "acId": "AC-55",
    "original": "The service performs canonical Prisma reads only; it does not query LanceDB, BM25, entity graph, or other derived stores",
    "refined": "Code inspection assertion: CanonicalStateService source contains zero references to 'lance', 'bm25', 'entityGraph', 'derivedStore', or any query method other than PrismaService calls. Integration test mocking all non-Prisma stores verifies they receive zero calls.",
    "testable": true,
    "storyId": "US-006"
  }
]