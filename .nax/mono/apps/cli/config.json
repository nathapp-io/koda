{
    "quality": {
        "commands": {
            "test": "bun run test",
            "testScoped": "bunx jest {{files}} --forceExit --passWithNoTests",
            "typecheck": "bun run type-check",
            "lint": "bun run lint",
            "lintFix": "bun run lint:fix",
            "formatFix": "bun run lint:fix"
        }
    }
}
