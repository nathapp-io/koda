Timestamp: 2026-04-07T02:22:34.988Z
Session:   nax-5166d743-vcs-phase3-pr-status-sync-vcs-p3-001-b-refine
Type:      complete
StoryId:   VCS-P3-001-B
Feature:   vcs-phase3-pr-status-sync
Stage:     complete
---
IMPORTANT: Your entire response must be a single JSON object or array. Do not explain your reasoning. Do not use markdown formatting. Output ONLY the JSON.

You are an acceptance criteria refinement assistant. Your task is to convert raw acceptance criteria into concrete, machine-verifiable assertions.

CODEBASE CONTEXT:


ACCEPTANCE CRITERIA TO REFINE:
1. VcsPrStatus interface has number, state, draft, merged, mergedAt, mergedBy, mergeSha, url, title fields with correct nullable types
2. IVcsProvider interface includes getPullRequestStatus(prNumber: number): Promise<VcsPrStatus> method
3. IVcsProvider interface includes listPullRequests(state?: 'open' | 'closed' | 'all'): Promise<VcsPrStatus[]> method
4. GitHubProvider.getPullRequestStatus(42) calls GET /repos/{owner}/{repo}/pulls/42 and maps the response to VcsPrStatus
5. GitHubProvider.getPullRequestStatus returns merged:true and populates mergedAt, mergedBy, mergeSha when GitHub PR is merged
6. GitHubProvider.getPullRequestStatus throws NotFoundAppException when GitHub returns 404
7. GitHubProvider.listPullRequests('open') calls GET /repos/{owner}/{repo}/pulls?state=open and returns array of VcsPrStatus
8. GitHubProvider.listPullRequests() defaults to state='open' when no argument is provided

For each criterion, produce a refined version that is concrete and automatically testable where possible.
Respond with a JSON array:
[{
  "original": "<exact original criterion text>",
  "refined": "<concrete, machine-verifiable description>",
  "testable": true,
  "storyId": ""
}]

Rules:
- "original" must match the input criterion text exactly
- "refined" must be a concrete assertion (e.g., "Array of length N returned", "HTTP status 200 returned")
- "testable" is false only if the criterion cannot be automatically verified (e.g., "UX feels responsive", "design looks good")
- "storyId" leave as empty string — it will be assigned by the caller

YOUR RESPONSE MUST START WITH { OR [ AND END WITH } OR ]. No other text.