Timestamp: 2026-04-07T02:25:10.171Z
Session:   nax-5166d743-vcs-phase3-pr-status-sync-vcs-p3-002-c-refine
Type:      complete
StoryId:   VCS-P3-002-C
Feature:   vcs-phase3-pr-status-sync
Stage:     complete
---
IMPORTANT: Your entire response must be a single JSON object or array. Do not explain your reasoning. Do not use markdown formatting. Output ONLY the JSON.

You are an acceptance criteria refinement assistant. Your task is to convert raw acceptance criteria into concrete, machine-verifiable assertions.

CODEBASE CONTEXT:


ACCEPTANCE CRITERIA TO REFINE:
1. VcsPollingService.poll() calls syncPrStatus() after issue sync completes on each polling tick
2. handleWebhook() with event 'pull_request' and action 'opened' updates TicketLink.prState to 'draft' if draft=true, otherwise 'open'
3. handleWebhook() with event 'pull_request', action 'closed', and merged=true updates prState to 'merged' and triggers the auto-transition logic from VCS-P3-002-B
4. handleWebhook() with event 'pull_request', action 'closed', and merged=false updates prState to 'closed'
5. handleWebhook() with event 'pull_request' and action 'ready_for_review' updates prState to 'open'
6. Webhook handlers look up TicketLink using the prNumber field from the webhook payload
7. If no TicketLink matches the prNumber, the webhook event is silently ignored
8. Unhandled pull_request actions are ignored without error

For each criterion, produce a refined version that is concrete and automatically testable where possible.
Respond with a JSON array:
[{
  "original": "<exact original criterion text>",
  "refined": "<concrete, machine-verifiable description>",
  "testable": true,
  "storyId": ""
}]

Rules:
- "original" must match the input criterion text exactly
- "refined" must be a concrete assertion (e.g., "Array of length N returned", "HTTP status 200 returned")
- "testable" is false only if the criterion cannot be automatically verified (e.g., "UX feels responsive", "design looks good")
- "storyId" leave as empty string — it will be assigned by the caller

YOUR RESPONSE MUST START WITH { OR [ AND END WITH } OR ]. No other text.