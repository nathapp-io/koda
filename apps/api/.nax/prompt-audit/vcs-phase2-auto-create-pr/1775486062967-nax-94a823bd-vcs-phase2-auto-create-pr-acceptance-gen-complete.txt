Timestamp: 2026-04-06T14:34:22.967Z
Session:   nax-94a823bd-vcs-phase2-auto-create-pr-acceptance-gen
Type:      complete
StoryId:   (none)
Feature:   vcs-phase2-auto-create-pr
Stage:     complete
---
You are a senior test engineer. Your task is to generate a complete acceptance test file for the "vcs-phase2-auto-create-pr" feature.

## Step 1: Understand and Classify the Acceptance Criteria

Read each AC below and classify its verification type (prefer runtime-check):
- **runtime-check** (PREFERRED): Import the module, call the function, assert on return values, thrown errors, or observable side effects. This is the strongest verification — use it whenever possible.
- **integration-check**: Requires a running service (e.g. HTTP endpoint returns 200, database query succeeds). Use setup blocks.
- **file-check** (LAST RESORT): Only for ACs that genuinely cannot be verified at runtime (e.g. "no banned imports in file X", "config file exists"). Never use file-check when a runtime import + assertion would work.

ACCEPTANCE CRITERIA:
AC-1: buildBranchName('KODA', 42, 'Fix login redirect bug') returns exact string 'koda/KODA-42/fix-login-redirect-bug'
AC-2: buildBranchName('PROJ', 1, 'A very long title that exceeds the maximum allowed length for branch names') returns a string where string.length <= 100
AC-3: buildBranchName('PROJ', 1, 'Special chars: @#$%^&*()') slug portion matches regex /^[a-z0-9]+(-[a-z0-9]+)*$/ (only alphanumeric and hyphens, no special characters)
AC-4: buildBranchName('PROJ', 1, 'trailing---hyphens---') slug portion does not end with hyphen character (last char !== '-')
AC-5: Object.getPrototypeOf(GitHubProvider).constructor.prototype includes method 'createPullRequest' with signature (CreatePrParams) => Promise<VcsPullRequest>
AC-6: Object.getPrototypeOf(GitHubProvider).constructor.prototype includes method 'getDefaultBranch' with signature () => Promise<string>
AC-7: GitHubProvider.getDefaultBranch() makes HTTP GET to https://api.github.com/repos/{owner}/{repo} and returns response.data.default_branch as string
AC-8: GitHubProvider.createPullRequest() calls HTTP POST to https://api.github.com/repos/{owner}/{repo}/git/refs with body containing ref (refs/heads/{branch}) and sha (default branch HEAD SHA)
AC-9: GitHubProvider.createPullRequest() calls HTTP POST to https://api.github.com/repos/{owner}/{repo}/pulls with body.draft === true and body.base === defaultBranch
AC-10: GitHubProvider.createPullRequest() return value is an object with properties: number (number), url (string), branchName (string), state (string), draft (boolean) - all present
AC-11: When HTTP POST to /git/refs returns status 422, createPullRequest() does not throw, continues to POST /pulls, and returns VcsPullRequest
AC-12: HttpClient interface/type includes method 'post' with signature (url: string, body?: object) => Promise<HttpResponse>
AC-13: VcsProviderConfig.httpClient satisfies HttpClient interface (has post method callable with url and body parameters)
AC-14: Assert vcsService.createPrForTicket(ticketId, projectId) is invoked when TicketStatusTransitionEvent has newStatus === 'VERIFIED' && project.vcsConnections contains connection where connection.isActive === true
AC-15: Assert branch name matches pattern `/^[A-Z]+-\d+-[a-z0-9-]+$/` and is computed by calling buildBranchName(project.key, ticket.number, ticket.title) where buildBranchName converts title to kebab-case and truncates to 50 chars
AC-16: Assert prTitle matches regex `/^[A-Z]+-\d+:\s+.+$/` constructed as `${project.key}-${ticket.number}: ${ticket.title}`
AC-17: Assert createPullRequest argument body equals ticket.description ?? '' (empty string when null or undefined)
AC-18: Assert GitHub provider.createPullRequest() is called with argument draft === true
AC-19: Assert TicketLink record is created with fields: url equals pr.html_url, provider equals 'github', externalRef matches `/^[\w-]+\/[\w-]+#\d+$/`
AC-20: Assert TicketActivity record is created where action === 'VCS_PR_CREATED' and entityType === 'TICKET' and entityId === ticket.id
AC-21: Assert ActivityType enum exports VCS_PR_CREATED: string value and enum file exists at apps/api/src/common/enums.ts
AC-22: Assert vcsService.createPrForTicket is never called when project.vcsConnections is empty or all connections have isActive === false
AC-23: Assert ticket.status === 'VERIFIED' in database after GitHub API throws HttpStatusError or NetworkError, and no exception propagates from TicketStatusService.updateStatus()
AC-24: Assert Logger.warn is called with message containing 'GitHub API error' or 'Failed to create PR' and error object includes message and status properties
AC-25: Assert vcsService.createPrForTicket is not called for status transitions to 'OPEN', 'IN_PROGRESS', 'RESOLVED', 'CLOSED' or any status !== 'VERIFIED'
AC-26: Assert both apps/api/src/i18n/en/vcs.json and apps/api/src/i18n/zh/vcs.json contain keys 'pr.created' and 'pr.createFailed' with non-empty string values

## Step 2: Explore the Project

Before writing any tests, examine the project to understand:
1. **Language and test framework** — check dependency manifests (package.json, go.mod, Gemfile, pyproject.toml, Cargo.toml, build.gradle, etc.) to identify the language and test runner
2. **Existing test patterns** — read 1-2 existing test files to understand import style, describe/test/it conventions, and available helpers
3. **Project structure** — identify relevant source directories to determine correct import or load paths



## Step 3: Generate the Acceptance Test File

Write the complete acceptance test file using the framework identified in Step 2.

Rules:
- **One test per AC**, named exactly "AC-N: <description>"
- **runtime-check ACs** (default) → import the module directly, call functions with test inputs, assert on return values or observable side effects (log calls, thrown errors, state changes)
- **integration-check ACs** → use the language's HTTP client or existing test helpers; add a clear setup block (beforeAll/setup/TestMain/etc.) explaining what must be running
- **file-check ACs** (last resort only) → read source files using the language's standard file I/O, assert with string or regex checks. Only use when the AC explicitly asks about file contents or imports — never use file-check to verify behavior that can be tested by calling the function
- **NEVER use placeholder assertions** — no always-passing or always-failing stubs, no TODO comments as the only content, no empty test bodies
- Every test MUST have real assertions that PASS when the feature is correctly implemented and FAIL when it is broken
- **Prefer behavioral tests** — import functions and call them rather than reading source files. For example, to verify "getPostRunActions() returns empty array", import PluginRegistry and call getPostRunActions(), don't grep the source file for the method name.
- **File output (REQUIRED)**: Write the acceptance test file DIRECTLY to the path shown below. Do NOT output the test code in your response. After writing the file, reply with a brief confirmation.
- **Path anchor (CRITICAL)**: Write the test file to this exact path: `/Users/subrinaai/Desktop/workspace/subrina-coder/projects/koda/repos/koda/apps/api/.nax/features/vcs-phase2-auto-create-pr/.nax-acceptance.test.ts`. Import from package sources using relative paths like `../../../src/...` (3 levels up from `.nax/features/<name>/` to the package root).