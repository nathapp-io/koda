// Stub — implementer must replace this with a real Joi validation schema.
// This file is intentionally incomplete so AC3 tests fail in the RED phase.
export function validate(config: Record<string, unknown>): Record<string, unknown> {
  return config;
}
