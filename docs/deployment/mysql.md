# MySQL / MariaDB Deployment Guide (DEPRECATED)

> **DEPRECATED:** PostgreSQL is now Koda's only database. MySQL and MariaDB are no longer
> supported providers. See the [PostgreSQL deployment guide](./postgresql.md).

This guide is kept only as a historical reference; its former MySQL/MariaDB setup instructions no longer apply.
