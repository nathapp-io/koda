# A historical SQLite deployment guide (DEPRECATED)

> **DEPRECATED:** This historical SQLite deployment guide is obsolete — PostgreSQL is now
> Koda's only database. See the [PostgreSQL deployment guide](./postgresql.md) and the stock
> `docker-compose.yml` at the repo root (which ships a `postgres:16` service).

This guide is kept only as a historical reference for pre-Postgres deployments.
